#include "search.h"
using namespace std;

template <typename T>
Search<T>::Search()
{}

template <typename T>
Search<T>::~Search()
{}

template <typename T>
void Search<T>::sort()
{}

template <typename T>
void Search<T>::fill_random(int N)
{}

template <typename T>
int  Search<T>::search_sequential(T val)
{
    return 0;
}